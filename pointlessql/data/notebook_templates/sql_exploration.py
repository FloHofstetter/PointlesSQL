# %% [markdown]
# # SQL exploration
#
# Drop a few `%sql` cells in to query the catalog without leaving
# Python.  Replace `main.public.example` with a real three-part name
# before running.

# %%
# %sql -o df SELECT 1 AS hello

# %%
df.head()
