// Page entry: AI/BI dashboard view (loaded via data-pql-entry on the
// authenticated page and as a static module tag on the public viewer).
import { biDashboardView } from '../pages/bi_dashboard/view.js';

window.biDashboardView = biDashboardView;
