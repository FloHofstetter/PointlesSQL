// Page entry: table detail governance widgets (certification +
// request access). The rest of the table page's factories are
// cross-page chrome that stays in bootstrap.js.
import { requestAccess, tableCertification } from '../pages/table_governance.js';

window.tableCertification = tableCertification;
window.requestAccess = requestAccess;
