# Training context

The `mlflow.autolog`-wrapping context manager that captures
hyperparameters, metrics, and hardware fingerprint on every
model training run.

::: pointlessql.services.agent_runs.training_context
    options:
      show_root_heading: false
      filters:
        - "!^_"
